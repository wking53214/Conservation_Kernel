# Hostile implementation report

## Current classification

| Claim | Classification | Evidence |
| --- | --- | --- |
| A typed artifact/envelope can be implemented | `TEST-VERIFIED` | Model and round-trip tests. |
| Deliberate unauthorized epistemic promotion is detected | `TEST-VERIFIED` | Hostile regression corpus. |
| Unauthorized authority escalation is detected | `TEST-VERIFIED` | Recommendation/decision and canonicalization attacks. |
| Provenance and lineage loss are detected | `TEST-VERIFIED` | Source stripping, source substitution, false lineage, and dropped proposition cases. |
| Uncertainty collapse is detected | `TEST-VERIFIED` | Unknown, estimated, and conflict-collapse cases. |
| Legitimate content transformation is permitted | `TEST-VERIFIED` | Ten deterministic summary transformations. |
| Full reconstruction from machine-readable history works | `TEST-VERIFIED` | Reconstruction tests over ten accepted transformations. |
| The mechanism independently verifies truth | `FALSIFIED` | It is explicitly outside the model's capability. |
| A library alone prevents bypass by a downstream system | `FALSIFIED` | A caller can create a new artifact without submitting it; a gateway/protocol is required. |
| The treatment is materially better than conventional controls in general | `UNVERIFIED` | The first deterministic run is a mechanism test, not a statistically powered comparison. |
| Novelty over existing provenance/audit systems | `UNVERIFIED` | No prior-art study or comparative experiment has been completed. |

The quantitative result is generated by `experiments/run_experiment.py` after
the test suite passes. No claim of external-world truth or universal
governance is made.
